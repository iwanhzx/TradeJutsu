{
  "name": "tradejutsu",
  "private": true,
  "scripts": {
    "dev": "concurrently --restart-tries -1 --restart-after 3000 -n BE,FE -c blue,green \"cd backend && uvicorn app.main:app --reload --reload-delay 1.5 --port 8000\" \"cd frontend && npm run dev\"",
    "backend": "cd backend && uvicorn app.main:app --reload --reload-delay 1.5 --port 8000",
    "frontend": "cd frontend && npm run dev",
    "test": "cd backend && python -m pytest tests/ -v",
    "seed": "cd backend && python -m scripts.seed_data"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
